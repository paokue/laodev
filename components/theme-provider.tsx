import * as React from 'react'

// Theme provider placeholder - can be replaced with a proper theme solution later
export function ThemeProvider({ children }: { children: React.ReactNode }) {
  return <>{children}</>
}
